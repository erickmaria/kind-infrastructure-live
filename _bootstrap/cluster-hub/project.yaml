apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: cluster-hub
  namespace: argocd
spec:
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
  description: hub project envrionment
  sourceRepos:
    - https://github.com/erickmaria/kind-infrastructure-live.git

---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: infrastructure
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/erickmaria/kind-infrastructure-live.git
        revision: HEAD
        directories:
          - path: clusters/cluster-hub/namespaces/*
  syncPolicy:
    preserveResourcesOnDeletion: true
  template:
    metadata:
      name: '{{nameNormalized}}.{{path.basename}}'
      labels:
        cluster: '{{name}}'
    spec:
      project: hub
      source:
        repoURL: https://github.com/erickmaria/kind-infrastructure-live.git
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: '{{server}}'
        namespace: '{{path.basename}}'
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - CreateNamespace=true